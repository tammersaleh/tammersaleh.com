- page_title @image
%p
  = link_to(image_tag(@image.image.url(:medium)), @image.image.url)
%p
  %b
    Description:
  = h @image.description
= link_to 'Edit', edit_image_path(@image)
|
= link_to 'Back', :back
