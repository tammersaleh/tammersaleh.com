---
bodyclass: newproject
---

.article_index
  - blog.articles.each do |article|
    .article
      %a{href: article.url}
        .background{"data-original" => index_article_background_url(article)}
        %h4= article.title

